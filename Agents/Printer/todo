things left to do
include style sheets for using the import tagged
sellout reformatting of an HTML with a different style directly from the
preview page
if make sure that the links printing from the cash works
make sure that the default style for document works
consider using HTML2PS as the main method for grabbing documents in
creating books
print out a summary sheet for longer documents
make the table of contents tough work correctly

at options for making previews, books, etc.

