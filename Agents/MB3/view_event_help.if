<!doctype html public "-//IETF//DTD HTML//EN//2.0">
<html><head>
<title>&agentName; Help</title>
<base href="http://&piaHOST;:&piaPORT;/&agentName;/home.if">
</head><body bgcolor="#ffffff" link="#c40026" vlink="#e40026">

<set entity name=title>Event Help</set>
<!-- put up the header at the top of the page -->
<read interform file="heading.inc" process>

<!-- put up the events for today and navigation buttons -->
<read interform file="subhead.inc" process>

<!-- load tags specific to this agent (just in case not already loaded) -->
<tagset-load name=MB3-actors>
<hr>

<dl>
  <dt> Local files
  <dd> Users can make shortcuts (links) to other types of documents, for
      example PowerPoint files.  First mount (map) the MB3 disk: <code><get
      agent name=shareDIR> </code>,  then copy the file(s) to that directory,
      then enter the filename and an identifier below.  (Eventually, we should
      automate this.)
  <dt> History
  <dd> If the proxy is enabled on the MB3, then any web documents which are
      viewed during a presentation on the MB3 will be stored by the  <a
      href=/History/home.if>History agent </a>
      (Eventually we should automatically add those links here.)
         Annotations (on screen drawings) saved to <code><get
	   name=annotationDIR agent></code> would also show up here. 
</dl>

<p>
<!-- generic agent information -->
<read interform file="about.inc" quiet process>



<hr>

<!-- shows/process the navigation bar -->
<expand><get agent name=navbar></expand>

 <!-- copyright and cvs source control information -->
<b>Copyright © 1998 Ricoh Silicon Valley</b><br>
<!-- $Id$ --><br>
</body></html>
